import { DocumentFilterPipe } from './document-filter.pipe';

describe('DocumentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
